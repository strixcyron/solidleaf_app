import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Basic smoke test', (WidgetTester tester) async {
    // Пока мы не пишем тесты интерфейса, оставляем простую проверку,
    // чтобы анализатор кода не ругался на отсутствие класса MyApp.
    expect(true, true);
  });
}